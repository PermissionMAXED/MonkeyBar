// Jungle Poker bot brain — NULL STUB (R2). The R6 mode agent replaces
// createBrain with a real factory ({seat, personalityId, rng}) → brain — see
// bots/brains/index.js for the decision surface. While null, botManager
// skips the seat and the gameRoom fallback drives it via engine.onTimeout.

export const MODE_ID = 'junglePoker';
export const createBrain = null;
