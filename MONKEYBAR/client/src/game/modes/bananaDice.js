// Banana Dice choreography — PLACEHOLDER (R4 fills this in: shell lifts on
// reveal, dice rattle, die-loss puff). Exporting null means gameClient.js
// runs base handling only (turn ring, penalty/cannon drama, resync). Export a
// `{ resync(snapshot, tools), handle(kind, p, tools) }` object (contract in
// game/modes/index.js) to take over.
export default null;
