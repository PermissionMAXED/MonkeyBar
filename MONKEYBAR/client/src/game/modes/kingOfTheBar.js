// King of the Bar choreography — PLACEHOLDER (R6 fills this in: Bar Rule
// gong + signage flourish on kingBarRule, Royal Decree fruit pick beat).
// Exporting null means gameClient.js runs base handling only — which for this
// ML-derived mode already covers the shared played/called/reveal/cannon drama.
// Export a `{ resync(snapshot, tools), handle(kind, p, tools) }` object
// (contract in game/modes/index.js) to take over.
export default null;
