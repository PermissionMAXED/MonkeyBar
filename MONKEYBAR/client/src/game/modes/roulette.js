// Coconut Roulette choreography — PLACEHOLDER (R4/R5 fills this in: coconut
// hand-offs, shake wobble + tick SFX, explosion). Exporting null means
// gameClient.js runs base handling only (turn ring, penalty/cannon drama,
// resync). Export a `{ resync(snapshot, tools), handle(kind, p, tools) }`
// object (contract in game/modes/index.js) to take over.
export default null;
