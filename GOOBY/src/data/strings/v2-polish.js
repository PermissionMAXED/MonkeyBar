// V2/G16: strings module stub (PLAN2 §E0.1-1) — OWNED BY AGENT G30.
// Scope: onboarding additions, What's-new panel, ship polish (PLAN2 §A3 checklist 12).
// G30 adds every key of that scope here (BOTH EN and DE — §A parity rule);
// nobody else edits this file, and src/data/strings.js itself stays untouched
// after wave 1 (it already spreads this module after all v1 entries).

/** @type {Record<string, string>} */
export const EN = {};

/** @type {Record<string, string>} */
export const DE = {};
