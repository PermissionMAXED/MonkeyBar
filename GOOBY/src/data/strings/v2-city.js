// V2/G16: strings module stub (PLAN2 §E0.1-1) — OWNED BY AGENT G21.
// Scope: city 2.0: vet clinic, vet trip flow, destination picker, landmarks (PLAN2 §C9).
// G21 adds every key of that scope here (BOTH EN and DE — §A parity rule);
// nobody else edits this file, and src/data/strings.js itself stays untouched
// after wave 1 (it already spreads this module after all v1 entries).

/** @type {Record<string, string>} */
export const EN = {};

/** @type {Record<string, string>} */
export const DE = {};
